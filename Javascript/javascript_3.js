function displayRole(character){
   var characterRole = character.getAttribute("data-character-role");
    alert (character.innerHTML + " is in the " + characterRole + " role!");
}

//function displayType(character) {
    //var characterType = character.getAttribute("data-character-type");
  //  alert(characterType + " is in the " + character.innerHTML + " universe!");
//}